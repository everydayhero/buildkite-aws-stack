#!/bin/bash -ie

echo "~~~ Validating :terraform:"
terraform validate

echo "~~~ Formatting :terraform:"
if [[ $(terraform fmt -list=true -write=false) ]]; then
  echo "Invalid format. Run 'terraform fmt' and commit changes."
  exit -1
fi

